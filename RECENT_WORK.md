# Crank Schedule 최근 작업 기록

최종 정리일: 2026-07-14  
작업 폴더: `C:\Crank\Recording files\Schedule-improved`  
원본 보존 폴더: `C:\Crank\Recording files\Schedule`

## 2026-07-14 연간 일정 검색 팝업

- 본문을 차지하던 sticky 검색창을 제거하고, 달력/리스트 보기 버튼 옆 `🔍` 버튼으로 여는 독립 모달 팝업으로 변경했다.
- 검색 범위는 현재 보고 있는 연도의 1월~12월이며 일정명과 시간을 검색한다.
- 결과는 날짜순으로 처음 12개를 표시하고 `더보기`로 12개씩 확장한다.
- 결과 선택 시 해당 월로 이동해 공개 페이지는 상세 모달, 관리자 페이지는 편집 모달을 연다.
- 바깥 영역 클릭, 닫기 버튼, Esc로 닫히며 달력 스크롤 및 고정 헤더 배치에 영향을 주지 않는다.

## 작업 원칙

- 원본 `Schedule` 폴더는 수정하지 않는다.
- 실제 수정은 `Schedule-improved`에서만 진행한다.
- 새 서비스나 복잡한 빌드 과정은 추가하지 않는다.
- 기존 배포 방식인 GitHub 정적 파일 업로드를 유지한다.
- Worker를 수정한 경우에만 기존 Cloudflare Worker에 `worker.js`를 다시 배포한다.
- 공개·관리자 달력의 레이아웃 및 다시보기 로직은 항상 같이 수정하고 비교 검사한다.

## 주요 파일

- `crank_schedule.html`: 공개 일정 페이지
- `admin.html`: 관리자 일정 편집 페이지
- `replay.html`: 다시보기 모음 페이지
- `schedule_css.css`: 공개·관리자 달력 공통 보조 스타일
- `worker.js`: 관리자 인증, GitHub 저장, 치지직 API 프록시
- `data/schedule_YYYY-MM.json`: 월별 일정
- `data/tournaments.json`: 대회 정보
- `assets/images/`: 아이콘과 자동 클릭용 이미지
- `auto_clicker.py`, `assets/images/button.png`: 별도 자동화 파일이므로 삭제 금지

## 현재 다시보기 우선순위

날짜별 다시보기 선택 순서는 아래와 같다.

1. 일정에 직접 입력한 수동 링크
2. 제목 날짜가 일정 날짜와 일치하는 YouTube 다시보기
3. 같은 날짜의 치지직 다시보기

날짜 파서는 다음 형식을 지원한다.

- `260711`
- `2026-07-11`
- `2026년 7월 11일`

수동 링크의 표시 출처는 `수동`이라는 별도 종류를 쓰지 않고 URL로 판별한다.

- YouTube URL: 우측 하단 빨간 `▶ YT`
- 치지직 URL: 우측 하단 초록 `▶ CHZZK`

별도의 빨강/초록 재생 아이콘은 표시하지 않고 출처 배지 하나로 합친다.

## 치지직 연동

- 라이브 배너에는 방송 제목과 카테고리만 표시하고 시청자 수는 표시하지 않는다.
- Worker의 채널 최근 영상 API에서 영상 번호별 썸네일 맵을 만든다.
- 수동 치지직 링크도 URL의 영상 번호로 이 맵에서 썸네일을 먼저 찾는다.
- 채널 목록에 없을 때만 `/api/chzzk/video/{videoNo}` 개별 조회를 보조로 사용한다.
- 공개 페이지와 관리자 페이지가 같은 링크와 썸네일을 사용한다.
- 로컬 치지직 테스트는 Worker 허용 출처에 맞춰 `http://localhost:8080` 또는 `http://127.0.0.1:8080`을 사용한다.
- 임의의 다른 포트에서는 로컬 CORS 오류가 날 수 있지만 GitHub Pages 오류와는 다르다.

## 반응형 보기

화면 선택은 PC, 태블릿, 휴대폰 구간별로 따로 기억한다.

- 1051px 이상: 달력형 기본
- 681~1050px: 리스트형 기본
- 680px 이하: 리스트형 기본

사용자가 직접 달력형 또는 리스트형을 선택하면 해당 폭 구간에서만 선택을 기억한다. 태블릿에서 달력을 골라도 휴대폰까지 달력형으로 강제되지 않는다.

좁은 화면에서 사용자가 달력형을 직접 선택하면 가독성을 위해 달력 안의 다시보기 썸네일은 숨긴다.

1051~1599px이면서 화면 높이가 700px 이상이면 달력 칸의 다시보기 썸네일을 칸 하단 전체 폭에 약 16:5 비율로 표시한다. 이미지는 `object-fit: cover`로 위아래가 일부 잘릴 수 있다.

## 일정 검색

- 공개·관리자 페이지에서 현재 보고 있는 달의 일정 제목과 시간을 검색한다.
- 최대 12개 결과를 날짜순으로 표시하고 나머지 결과 개수도 안내한다.
- 검색 결과를 누르면 해당 날짜의 상세창 또는 편집창을 연다.
- Escape 또는 지우기 버튼으로 검색을 닫을 수 있다.
- 검색창은 일정 상단바 아래에 sticky로 유지된다.

## 상단 고정 영역

- 고정 순서는 `치지직 라이브 배너 → 일정 상단바 → 검색창`이다.
- 라이브 배너와 일정 상단바의 실제 높이를 `ResizeObserver`로 계산한다.
- `--live-banner-height`, `--schedule-header-height` CSS 변수를 이용해 스크롤 중 겹치지 않게 한다.
- 리스트의 오늘 자동 스크롤도 라이브 배너·일정 상단바·검색창 높이를 모두 반영한다.

## 우측 상단 특수 배지

아래 일정은 일반 일정 태그가 아니라 달력 칸 우측 상단에 별도 상태 배지로 표시한다.

- `휴방` 포함: `휴방`
- `올리비아 출국` 포함: `올출`
- `올리비아 출장` 포함: `올출`
- `올리비아 귀국` 포함: `올귀`
- `올리비아 입국` 포함: `올귀`

좁은 달력에서는 30×18px 두 글자 배지를 사용한다. `출장`을 특수 키워드에서 빼먹으면 일반 일정 영역으로 내려가는 버그가 다시 생기므로 주의한다.

## 관리자 페이지 개선 사항

- 관리자 세션은 브라우저에 저장하고 Worker 토큰 유효기간은 30일이다.
- 자물쇠 버튼으로 로그인 또는 로그아웃한다.
- 저장 상태를 화면에 표시한다.
  - 변경 없음
  - 저장되지 않은 변경사항
  - 저장 중
  - 저장 완료 시간
  - 저장 실패
- 저장 결과는 토스트로 표시한다.
- 저장되지 않은 상태에서 편집창을 닫거나 페이지를 나가면 경고한다.
- 변경 취소 시 해당 날짜의 편집 전 데이터를 복구한다.
- `다시보기 새로고침` 버튼과 전용 로직은 사용자 요청으로 제거했다.
- `빈 날짜 숨기기/표시` 기능이 있으며 선택은 브라우저에 기억된다.

## 상세창과 접근성

- 달력 칸과 리스트 날짜 행은 Enter 또는 Space로 열 수 있다.
- 상세창은 `role="dialog"`, `aria-modal="true"`를 사용한다.
- 상세창을 열면 닫기 버튼으로 포커스가 이동한다.
- Escape로 닫을 수 있다.
- 닫으면 원래 선택했던 날짜 칸으로 포커스가 돌아간다.
- 키보드 포커스에는 청록색 외곽선을 표시한다.

## 다시보기 페이지

- 모바일에서도 영상 제목을 숨기지 않고 두 줄로 표시한다.
- 400px 이하에서는 제목 영역 높이 40px, 제목 글자 9px을 사용한다.
- 모바일 3열 구조와 하단 고정 내비게이션을 유지한다.

## 보안과 키

- GitHub 토큰과 관리자 비밀번호 등 Worker 비밀값은 HTML에 넣지 않는다.
- Worker Secret은 Cloudflare에 저장한다.
- YouTube 브라우저 API 키는 현재 HTML에서 직접 사용한다.
- 키 값 자체를 문서에 복사하지 않는다.
- Google Cloud에서 반드시 GitHub Pages 리퍼러와 YouTube Data API v3로 사용 범위를 제한한다.

## 최근 검증 결과

- 공개·관리자 페이지의 날짜별 다시보기 링크 일치 확인
- 수동 YouTube 링크는 빨간 `YT` 확인
- 수동 치지직 링크는 초록 `CHZZK` 및 실제 썸네일 로딩 확인
- 2026년 5월 데이터에서 특수 배지 확인
  - 17일 `올리비아 출장 → 올출`
  - 24일 `올리비아 귀국 → 올귀`
  - 25일 `휴방 → 휴방`
  - 28일 `올리비아 출장 → 올출`
- PC CSS 폭 1265px: 달력형과 하단 가로 썸네일 정상
- 태블릿 CSS 폭 985px: 리스트형 기본, 수동 달력 선택 가능
- 모바일 CSS 폭 375px: 리스트형 기본
- 모든 검사 폭에서 가로 넘침 없음
- 다시보기 카드 30개와 모바일 제목 표시 확인
- HTML 내 JavaScript, Worker, Python, JSON 문법 검사 통과
- 중복 ID, 누락 로컬 파일, 깨진 이미지 없음

## 배포 방법

정적 화면만 수정했을 때는 보통 다음 파일만 GitHub에 덮어쓴다.

- `crank_schedule.html`
- `admin.html`
- `replay.html`
- `schedule_css.css`

이번까지의 화면 변경에는 Worker 재배포가 필요 없다. `worker.js`를 실제로 수정한 경우에만 기존 Worker를 다시 배포한다.

CSS를 수정하면 `crank_schedule.html`과 `admin.html`의 `schedule_css.css?v=...` 버전도 올려 브라우저 캐시를 갱신한다.

## 다음 작업자가 먼저 확인할 것

1. 작업 위치가 `Schedule-improved`인지 확인한다.
2. 원본 `Schedule`의 추적 파일에 변경이 없는지 확인한다.
3. 공개·관리자 양쪽에 같은 다시보기 및 특수 배지 수정이 들어갔는지 확인한다.
4. 로컬 서버는 치지직 테스트가 필요하면 포트 8080을 사용한다.
5. 1280×768, 1000×768, 390×844 정도에서 각각 확인한다.
6. 공개·관리자 링크 맵이 같은지 비교한다.
7. 작업 후 임시 서버 로그와 `__pycache__`를 삭제한다.
